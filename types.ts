export interface FeatureItem {
  title: string;
  copy: string;
}

export interface NavItem {
  label: string;
  href: string;
}

export interface GalleryItem {
  id: number;
  src: string;
  alt: string;
}